/**
 * @param {number} number
 * @param {string} unit
 * @param {{time?: boolean, length?: boolean, angle?: boolean}} options
 */
export default function _default(number: number, unit: string, { time, length, angle }: {
    time?: boolean;
    length?: boolean;
    angle?: boolean;
}): string;
